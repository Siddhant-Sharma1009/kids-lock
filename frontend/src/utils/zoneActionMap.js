const zoneActionMap = {
  topLeft: (set) => set("illusion"),
  topRight: (set) => set("color"),
  bottomLeft: (set) => set("sound"),
  bottomRight: (set) => set("game"),
};

export default zoneActionMap;
